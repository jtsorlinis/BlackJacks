To build:

```bash
zig build -Doptimize=ReleaseFast
```

To run:

```bash
zig build run -Doptimize=ReleaseFast -- <rounds>
```

Or run the built binary directly:

```bash
zig-out/bin/ZigBlackJack <rounds>
```
