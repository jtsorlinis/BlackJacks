To build:

```bash
crystal build --release src/main.cr -o bin/CrystalBlackJack
```

To run:

```bash
crystal run --release src/main.cr -- <rounds>
```

Or run the built binary directly:

```bash
bin/CrystalBlackJack <rounds>
```
